import type { Provenance } from './types';

export type { Hop, Provenance, RequestMeta } from './types';

export function install(): void {}

export function camefrom(_target: Node | null): Provenance | null {
    return null;
}
